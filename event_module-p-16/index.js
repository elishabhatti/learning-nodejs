// event module - node.js
// event emitters is a core module in ode.js used to create and handle custom events.
// it is part of the evensts module and is often used for building event-driven systems in node.js

// key methods
// .emit (eventName, [args])
// purpose: emits (or triggers) an event with the spexfied eventName You can also pass arguments that will be consumed bu the listenser
// its like calling a functions but instead it triggers all listeners (functions) attacheched to the specified event

// .on(eventName, listener)
// purpse: apttaches a listener (a function) to a specific eventName. the listener will execute
// is emitted.

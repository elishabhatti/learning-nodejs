// import math from "./math.js"; // Import the `math` module
// import * as math from './math.js'

// console.log(math.add(2, 2)); // Use `math.add` to call the `add` function
// console.log(subtract(2, 2)); // Use `math.add` to call the `add` function
// console.log(multiply(2, 2)); // Use `math.add` to call the `add` function
// console.log(division(2, 2)); // Use `math.add` to call the `add` function
